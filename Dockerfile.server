FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install --production

# Copy server code
COPY server ./server

# Expose port
EXPOSE 4000

# Run migrations and start server
CMD ["sh", "-c", "node server/database/migrate.js && node server/server.js"]
